#ifndef __PPP_GPRS_H__
#define __PPP_GPRS_H__

/*-----------------------------------------
* 函数：gprs_start_connect
* 功能：后台连接GPRS上网,需要后台进程连接
* 参数：none
* 返回：>=0  
*-------------------------------------------*/
int gprs_start_connect(void);

#endif